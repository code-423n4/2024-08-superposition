use stylus_sdk::alloy_primitives::{Address, U256};

pub fn take(pool: Address, amount: U256) -> Result<(), Vec<u8>> {
    Ok(())
}

pub fn give(token: Address, amount: U256) -> Result<(), Vec<u8>> {
    Ok(())
}
